interface User {
    firstName,
    lastName: string,
    age: number
}

enum countries {
    'ROMANIA',
    'GERMANY'
}

const anuntam = (message: string, shouldAnnounce?) : void => {
    console.log(message);
}

const userValid = (user: User): boolean => {
//...
countries.GERMANY;
return false;
}